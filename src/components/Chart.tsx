// import { useLocation } from "react-router-dom";

interface ChartProps {
  coinId: string;
}
function Chart() {
  // const { pathname } = useLocation();
  return <h1>This is chart</h1>;
}

export default Chart;
